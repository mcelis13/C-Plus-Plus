#include <iostream>

using namespace std;

int main()
{
    int length = 5;
    int width = 10;
    int area = length * width;
    /* area *= 2;   Multiplying the area times 2 is shown by the following symbol "*="*/
    cout << length * width << endl; /* print out the product(multiply) of length times width "*" multiplication symbol.*/
    cout << length * 5 << endl;
    cout << area << endl;
}
