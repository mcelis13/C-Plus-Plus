#include <stdio.h>
#include <string.h>

int getname(void)
{
	char name[50];
        printf("State your name:");
        scanf("%s", &name);
        printf("Hello, World %s\n",name);
        return 0;
}

int addnumbers(void)
       int number[5];
       int*[]=x;
       int*[]=y;
{
       printf("Please enter a number:");
       scanf("%d"=x);
       printf("Please enter another number:");
       scanf("%d"=y); 
       printf("Calculating...%d\n", (x+y));
}

int main(void) 
{
	addnumbers(int argc, argv[]);
}
